version: '3.7'

services:
  app:
    build:
      context: .
      target: docker-compose
    stdin_open: true
    volumes:
      - .:/app
    tmpfs:
      - /app/tmp
      - /app/vendor
    depends_on:
      - mysqldb
