version: '3.7'

services:
  app:
    build:
      context: .
      target: app
      args:
        user: "${CONTAINER_USER:-app}"
    stdin_open: true
    volumes:
      - .:/app
    tmpfs:
      - /app/tmp
      - /app/vendor
